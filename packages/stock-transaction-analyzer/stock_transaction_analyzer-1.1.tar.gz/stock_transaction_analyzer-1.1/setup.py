from setuptools import setup, find_packages

setup(
    name='stock_transaction_analyzer',
    version='1.1',
    packages=['stock_transaction_analyzer'],
    include_package_data=True,
    install_requires=[
        # Add any dependencies here
    ],
)
