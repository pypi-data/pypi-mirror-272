[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mtok"
version = "5.0.0"
authors = [{ name = "Mahdi Mir", email = "imahdimir@gmail.com" }]
description = "Gets a token by key"
readme = "README.md"
dependencies = [
    "requests",
]
