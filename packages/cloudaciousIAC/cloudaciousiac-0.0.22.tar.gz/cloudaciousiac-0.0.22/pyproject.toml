[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cloudaciousIAC"
dynamic = ["version"]

[tool.hatch.build.targets.wheel]
packages = [
    "src/cloudaciousIAC",
    ]

[tool.hatch.version]
source = "env"
variable = "CI_COMMIT_TAG"
