project('windows-build', 'c', 'cpp',
    default_options: [
        'default_library=static',
        'wrap_mode=default',
        'buildtype=release',
        'pkgconf:tests=false',
    ],
)

subproject('pkgconf')
subproject('plutobook')
