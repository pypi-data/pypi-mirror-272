project('windows-build', 'c', 'cpp',
    default_options: [
        'wrap_mode=default',
        'buildtype=release',
        'pkgconf:tests=false',
    ],
)

subproject('pkgconf')
subproject('plutobook')
