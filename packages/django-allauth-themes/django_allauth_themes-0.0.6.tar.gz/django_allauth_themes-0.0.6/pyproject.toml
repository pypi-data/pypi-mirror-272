[build-system]
requires = ["setuptools >= 69.5"]
build-backend = "setuptools.build_meta"

[project]
name = "django-allauth-themes"
version = "0.0.6"
description = "Themes for Django-Allauth"
readme = "README.md"
requires-python = ">=3.7"
license = {file = "LICENSE"}
authors = [
  {name = "stackmuse", email = "stackmuse@github.com" }
]

dependencies = [
  "django-allauth"
]

[project.urls]
"Homepage" = "https://github.com/stackmuse/django-allauth-themes"
"Issues" = "https://github.com/stackmuse/django-allauth-themes/issues"
