from distutils.core import setup
# import setuptools

packages = ['pov_city']
setup(name='pov_city',
      version='0.0.1',
      author='xigua, 中国城市/城市等级/区域划分',
      packages=packages,
      package_dir={'requests': 'requests'}, )
