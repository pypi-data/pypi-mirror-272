[tool.poetry]
name = "Cystem"
version = "0.1.0"
description = "A Package Manager and build for C++ / C"
authors = ["NightProg"]
license = "GNU"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
click = "^8.1.7"
lark = "^1.1.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
