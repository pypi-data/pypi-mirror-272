[tool.poetry]
name = "compas-lcmtypes"
version = "0.1.1"
description = "CoMPAS LCM types"
authors = ["Kevin Barnard <kbarnard@mbari.org>"]
packages = [{include = "compas_lcmtypes"}]
include = [{path = "compas_lcmtypes/**/*.py"}]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"