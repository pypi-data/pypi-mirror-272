from setuptools import setup

setup(name = 'distributionsTCtest',
      version = '0.1',
      description = 'Gaussian distributions',
      packages = ['distributionsTCtest'],
      zip_safe=False)
