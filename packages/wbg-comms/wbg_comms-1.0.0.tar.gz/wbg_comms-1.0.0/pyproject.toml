[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[tool.black]
skip-string-normalization = true

[project]
name = 'wbg-comms'
version = '1.0.0'
requires-python = '>=3.10'
dependencies = [
    'wbg-core >=1.0.0, <2.0.0',
    'wbg-sharepoint >=1.0.0, <2.0.0',
]
