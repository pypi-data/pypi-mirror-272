[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[tool.black]
skip-string-normalization = true

[project]
name = 'dataframe-formatters'
version = '0.0.2'
requires-python = '>=3.10'
dependencies = [
    'pandas >=2.0.0,<3.0.0',
    'openpyxl >=3.1.2, <4.0.0'
]
