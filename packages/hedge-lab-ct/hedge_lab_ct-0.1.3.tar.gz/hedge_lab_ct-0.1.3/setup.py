from setuptools import setup, find_packages

setup(
    name='hedge-lab-ct',
    version='0.1.3',
    packages=find_packages(),
    long_description=open('README.md').read(),
    long_description_content_type='text/markdown',  # Adjust based on your markup type
)
