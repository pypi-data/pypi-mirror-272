[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
target-version = ['py37', 'py38', 'py39', 'py310']

[tool.isort]
profile = "black"

# Uncomment for capturing more logs during testing.
# Especially useful for asyncio tests that run a server in the background.
#[tool.pytest.ini_options]
#log_cli = true
#log_cli_level = "INFO"
